<UserControl
    x:Class="GZ_SpotGateEx.UCChannel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:GZ_SpotGateEx"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Height="50"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="24" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="Label">
            <Setter Property="Margin" Value="5,0,0,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Width" Value="80" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

    </UserControl.Resources>
    <StackPanel Orientation="Vertical">

        <StackPanel Margin="0,5,0,0" Orientation="Horizontal">
            <Label Content="通道:" />
            <TextBlock
                Width="30"
                VerticalAlignment="Center"
                Text="{Binding No}" />

            <Label Content="名称:" />
            <TextBox Width="120" Text="{Binding Name}" />

            <Label Content="工控机:" />
            <TextBlock VerticalAlignment="Center" Text="{Binding ClientIp}" />

            <Label Content="人脸(入):" />
            <TextBlock Text="{Binding FaceInIp}" />

            <Label Content="摄像机(入):" />
            <TextBlock Text="{Binding CameraInIp}" />

            <Label Content="平板(入):" />
            <TextBlock Text="{Binding PadInIp}" />

            <Label Content="人脸(出):" />
            <TextBlock Text="{Binding FaceOutIp}" />

            <Label Content="摄像机(出):" />
            <TextBlock Width="100" Text="{Binding CameraOutIp}" />

            <Label Content="平板(出):" />
            <TextBlock Text="{Binding PadOutIp}" />

            <Label Content="入(常开):" />
            <CheckBox />

            <Label Content="出(常开):" />
            <CheckBox />

        </StackPanel>
        <Separator Margin="0,8,0,0" Background="#FFCCCCCC" />
    </StackPanel>
</UserControl>
